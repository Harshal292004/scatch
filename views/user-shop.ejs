<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Listing</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: 'var(--color-primary)',
                        secondary: 'var(--color-secondary)',
                        accent: 'var(--color-accent)',
                    },
                },
            },
        }
    </script>
    <style>
        :root {
            --color-primary: #18181b;
            --color-secondary: #27272a;
            --color-accent: #3b82f6;
        }
        .light-theme {
            --color-primary: #f4f4f5;
            --color-secondary: #e4e4e7;
            --color-accent: #3b82f6;
        }
    </style>
</head>
<body class="bg-primary text-white flex items-center justify-center min-h-screen min-w-full transition-colors duration-300">
    <div class="min-h-screen min-w-full">
        <h2 class="text-xl font-bold mb-4 block p-10">Welcome</h2>
        <div class="grid grid-cols-3 gap-6 p-6">
            <div class="sortingandfilter col-span-1 bg-secondary rounded-lg shadow-lg p-4">
                <p class="font-bold italic text-md">Sort and Filter</p>
            </div>
            <div class="productdisplay col-span-2 bg-secondary rounded-lg shadow-lg p-4 flex flex-row flex-wrap">
                <% products.forEach((product) => { %>
                    <div class="product bg-white text-gray-800 rounded-lg shadow-md overflow-hidden m-2" style="background-color: <%= product.backgroundColor %>; color: <%= product.textColor %>;">
                        <div class="relative h-40 overflow-hidden">
                            <img src="data:image/jpeg;base64,<%= product.picture.toString('base64') %>" alt="Product Image" class="w-full h-full object-cover">
                            <div class="absolute top-2 right-2 bg-accent text-white px-2 py-1 rounded-full text-xs">
                                <%= product.inStock ? 'In Stock' : 'Out of Stock' %>
                            </div>
                        </div>
                        <div class="p-4">
                            <h3 class="text-lg font-semibold mb-2"><%= product.productName %></h3>
                            <p class="text-sm text-gray-600 mb-2"><%= product.productDescription %></p>
                            <div class="flex justify-between items-center mb-2">
                                <% if (product.brand && product.brand.isApplicable) { %>
                                    <div class="text-sm">
                                        <span class="font-bold">Brand:</span> <%= product.brand.name %>
                                    </div>
                                <% } %>
                                <div class="text-sm">
                                    <span class="font-bold">Category:</span> <%= product.category %>
                                </div>
                            </div>
                            <% if (product.material) { %>
                                <p class="text-sm mb-2"><span class="font-bold">Material:</span> <%= product.material %></p>
                            <% } %>
                            <% if (product.sizes && product.sizes.length > 0) { %>
                                <div class="flex flex-wrap gap-2 mb-2">
                                    <% product.sizes.forEach((size) => { %>
                                        <span class="text-xs bg-gray-200 px-2 py-1 rounded"><%= size %></span>
                                    <% }) %>
                                </div>
                            <% } %>
                            <% if (product.colors && product.colors.length > 0) { %>
                                <div class="flex flex-wrap gap-2 mb-2">
                                    <% product.colors.forEach(color => { %>
                                        <span class="w-4 h-4 rounded-full" style="background-color: <%= color %>"></span>
                                    <% }) %>
                                </div>
                            <% } %>
                            <% if (product.tags && product.tags.length > 0) { %>
                                <div class="flex flex-wrap gap-2 mb-2">
                                    <% product.tags.forEach(tag => { %>
                                        <span class="text-xs bg-gray-200 px-2 py-1 rounded"><%= tag %></span>
                                    <% }) %>
                                </div>
                            <% } %>
                            <div class="flex items-center mb-2">
                                <% if (product.reviews && product.reviews.length > 0) { %>
                                    <div class="flex text-yellow-400">
                                        <%= product.rating %> ★
                                    </div>
                                    <span class="text-sm ml-1">(<%= product.reviews.length %> reviews)</span>
                                <% } else { %>
                                    <span class="text-sm">No reviews yet</span>
                                <% } %>
                            </div>
                            <% if (product.quantity < 10) { %>
                                <p class="text-sm text-red-500 mb-2">Only <%= product.quantity %> left!</p>
                            <% } %>
                            <div class="flex justify-between items-center">
                                <span class="text-lg font-bold">$<%= product.price.toFixed(2) %></span>
                                <button class="bg-accent text-white px-4 py-2 rounded hover:bg-blue-600">
                                    Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
        <button id="themeToggle" class="fixed bottom-4 right-4 p-2 rounded-full bg-accent text-white">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
            </svg>
        </button>
    </div>
    <script>
        const themeToggle = document.getElementById('themeToggle');
        const body = document.body;
        themeToggle.addEventListener('click', () => {
            body.classList.toggle('light-theme');
        });
    </script>
</body>
</html>